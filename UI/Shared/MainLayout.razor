@inherits LayoutComponentBase

<MudThemeProvider IsDarkMode="true" Theme="BladesTheme"/>

<MudLayout>
	<MudAppBar Elevation="1">
		<MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
		<MudText Typo="Typo.h5" Class="ml-3">Sheets in the Dark</MudText>
		<MudSpacer />
	</MudAppBar>
	<MudDrawer @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
		<MudText Style="display: flex; justify-content: right; margin-right: 10px" Typo="Typo.caption">Version 6</MudText>
		<MudNavLink Href="/">Home</MudNavLink>
		<MudNavLink Href="/new/">New Character</MudNavLink>
	</MudDrawer>
	<MudMainContent>
		@Body
	</MudMainContent>
</MudLayout>

@code
{
	bool _drawerOpen = false;

	void DrawerToggle()
	{
		_drawerOpen = !_drawerOpen;
	}

	MudTheme BladesTheme { get; } = new MudTheme
	{
		//PaletteDark = new Palette
		//{
		//	Primary = Colors.DeepPurple.Darken4,
		//	TextSecondary = Colors.Orange.Darken3,
		//	TextPrimary = Colors.Amber.Lighten4
		//}
	};
}